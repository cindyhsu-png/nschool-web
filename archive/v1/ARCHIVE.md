# archive/v1 — nSchool 官網 v1.0（封存，不部署）

Iron 交付的 **v1.0** 版本首頁，僅作歷史封存，**不是**正式站內容。
正式站是 repo 根目錄的 v2.0（`../../index.html`）。

- `index.html` — v1 首頁（單檔，樣式在 `assets/`）
- `assets/` — v1 的 css / js
- `img/` — v1 引用到的素材

## 已移除的檔案

原始 zip 內以下檔案為設計稿／重複素材，未被 `index.html` 引用，為了控制 repo 體積已移除：

| 檔案 | 大小 | 原因 |
|---|---|---|
| `img/nshcool_style.svg` | 14 MB | 設計稿，網頁未引用 |
| `img/nshcool_style.png` | 2.4 MB | 同上 |
| `img/og_image.png` | 2.2 MB | 與根目錄 `img/og_image.png` 重複 |

需要原始檔請回 Google Drive 的 `nschool v1.0` 壓縮檔取用。
